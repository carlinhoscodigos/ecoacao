# Copie para .env.local em desenvolvimento.
# Em producao, o browser usa /api no mesmo dominio e a Vercel faz proxy para o backend.

# Backend local/dev ou URL publica via Tailscale Funnel
VITE_API_URL=https://desktop-95s8v63.tailb652ef.ts.net/

# Igual ao IMPORT_SECRET do backend - so para a migracao unica do localStorage (pode remover depois)
# VITE_IMPORT_SECRET=
