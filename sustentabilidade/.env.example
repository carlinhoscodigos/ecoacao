# Copie para .env.local (dev) ou defina no painel Vercel → Settings → Environment Variables
# (obrigatório em produção: o build embute VITE_* no bundle)

# Backend local (dev) ou URL pública via Tailscale Funnel
VITE_API_URL=http://localhost:3000

# Igual ao IMPORT_SECRET do backend — só para a migração única do localStorage (pode remover depois)
# VITE_IMPORT_SECRET=
